[package]
name = "mail-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
thiserror = "2"

# 异步流处理
futures = "0.3"

# IMAP
async-imap = "0.9"
async-native-tls = "0.5"
imap-proto = "0.16"
tokio = { version = "1", features = ["net", "sync", "macros", "io-util"] }
tokio-util = { version = "0.7", features = ["compat"] }

# MIME 解析
mailparse = "0.15"

# 字符编码解码（GBK, GB2312 等）
encoding_rs = "0.8"

# Base64 解码（MIME encoded-word）
base64 = "0.22"

# IMAP modified UTF-7 编解码
utf7-imap = "0.3"

# SMTP
lettre = { version = "0.11", default-features = false, features = [
    "builder",
    "smtp-transport",
    "tokio1-native-tls",
] }

# 日志
tracing = "0.1"
